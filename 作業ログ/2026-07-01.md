# 作業ログ 2026-07-01

## やったこと

### 1. Flutter開発環境のセットアップ完了

- **Android SDK Command-line Tools を展開・配置**
  - `%TEMP%\cmdline-tools.zip` を `%LOCALAPPDATA%\Android\Sdk\cmdline-tools\latest` に解凍
- **Android SDKライセンス同意**
  - `sdkmanager --licenses` で全7ライセンスに同意
- **Android SDKコンポーネントのインストール**
  - `platform-tools`, `build-tools;35.0.0`, `platforms;android-35` をインストール
  - `flutter doctor` の指摘に従い `platforms;android-36`, `build-tools;28.0.3` も追加
- **環境変数PATHへの追加**（ユーザー環境変数として恒久設定）
  - `C:\flutter\bin`
  - `%LOCALAPPDATA%\Android\Sdk\platform-tools`
  - `%LOCALAPPDATA%\Android\Sdk\cmdline-tools\latest\bin`
  - `C:\Program Files\Android\Android Studio\jbr\bin`
- **環境変数 ANDROID_HOME / ANDROID_SDK_ROOT の設定**
- **`flutter doctor` 最終確認**: Flutter・Windows・Android toolchain・Chrome・接続デバイスすべてグリーン（Visual Studioのみ×、Windowsアプリ開発用なので今回は不要）

### 2. Flutterプロジェクトのスキャフォールドと依存パッケージ追加

- `flutter create . --platforms android` でプロジェクト作成
- `pubspec.yaml` に以下を追加:
  - `drift` / `sqlite3_flutter_libs` / `path_provider` / `path` （ローカルDB）
  - `flutter_riverpod` / `riverpod_annotation` / `riverpod_generator` （状態管理）
  - `uuid` （クライアントサイドUUID生成）
  - `speech_to_text` / `permission_handler` （音声認識）
  - `table_calendar` / `intl` （カレンダーUI・日本語書式）
  - `drift_dev` / `build_runner` （開発用コード生成）

### 3. Drift（ローカルDB）の実装

- `lib/data/models/schedule_event.dart` — `ScheduleEvents` テーブル定義
  - フィールド: id (UUID PK), title, location, startAt, endAt, isAllDay, notes, createdAt, updatedAt, deletedAt（ソフトデリート）, sourceType, rawTranscript, syncStatus
  - 将来の端末間同期を見据えた設計（UUID PK・UTCタイムスタンプ・ソフトデリート）
- `lib/data/db/app_database.dart` — AppDatabase クラス
- `lib/data/db/app_database.g.dart` — Drift コード生成（build_runner実行）
  - ※プロジェクトがOneDriveフォルダ内にあるため、build_resolversのSQLiteファイルロック問題が発生
  - 対策: `.dart_tool\build_resolvers` にWindows Junctionポイントを作り、OneDrive外のパス（`C:\flutter_build_cache\`）に逃がして解決
  - `--define "riverpod_generator:riverpod_generator=enabled=false"` オプションで干渉を回避しDriftのみ生成
- `lib/data/repositories/schedule_repository.dart` — CRUD + watchAll + softDelete

### 4. プロバイダー実装（Riverpod）

- `lib/features/providers/schedule_providers.dart`
  - `databaseProvider`, `scheduleRepositoryProvider`, `scheduleEventsProvider`（StreamProvider）

### 5. 日本語日時パーサー実装（Step 2）

- `lib/domain/parsing/jp_datetime_parser.dart` — ルールベースパーサー
  - 対応表現: 絶対日付（N月N日）→ 相対日（今日/明日/明後日/昨日）→ 週+曜日（今週/来週/再来週）→ 単独曜日 → 明示時刻（午前/午後/N時N分）→ 曖昧時刻語（朝/夕方/夜 等）→ 場所（「場所は◯◯」）
  - 解析順序はプラン通りの優先順位
  - 曖昧時刻語は `warnings` に記録して確認画面で表示
- `lib/domain/parsing/parsed_schedule_result.dart` — 結果データクラス
- ユニットテスト `test/domain/parsing/jp_datetime_parser_test.dart` — **全11テストパス**
  - 「来週火曜の15時に会議」「明日の午後3時に歯医者」「今日19時から飲み会」「8月3日に出張」「場所は新宿」等

### 6. 音声入力ドメイン実装（Step 4）

- `lib/domain/voice/speech_service.dart` — `speech_to_text` ラッパー（`ja_JP`ロケール）

### 7. UI実装（Step 3・5）

- `lib/features/schedule_list/schedule_list_screen.dart` — 予定一覧（Riverpod StreamProvider、タップで編集、削除ボタン）
- `lib/features/confirm/confirm_event_screen.dart` — 確認・編集フォーム（音声入力・手動追加共通、低確度警告カード表示）
- `lib/features/voice_input/voice_input_screen.dart` — マイクボタン画面（押して話す→停止→確認画面へ、30秒安全タイマー付き）
- `lib/main.dart` / `lib/app.dart` — エントリポイント（ProviderScope・intlロケール初期化）

### 8. Androidマニフェスト更新

- `RECORD_AUDIO`, `INTERNET`, `BLUETOOTH_CONNECT` 等のパーミッションを追加

## 検証結果

- `dart analyze lib/` → エラー・警告ゼロ
- `flutter test test/domain/parsing/jp_datetime_parser_test.dart` → 11/11 テストパス

## 既知の注意点・制約

- **build_runner / OneDrive競合問題**: `.dart_tool\build_resolvers` をOneDrive外のJunctionに変更済み。今後Driftテーブル定義を変更したら以下のコマンドで再生成:
  ```
  flutter pub run build_runner build --delete-conflicting-outputs --define "riverpod_generator:riverpod_generator=enabled=false"
  ```
  また `build_resolvers` のJunctionが消えていたら再作成が必要（手順は作業ログ参照）
- **`flutter analyze`コマンド不可**: プロジェクトパスに日本語文字（`仮schedule-app`）が含まれるため、LspチャンネルのJSONパースがクラッシュする。代わりに `dart analyze lib/` を使用
- **エミュレータのAPKインストール**: `flutter run` はパス内の日本語文字のため `aapt` が失敗する。回避策として APK を ASCII パス（`C:\flutter_build_cache\app-debug.apk`）にコピーし `adb install` で手動インストール

### 9. Androidエミュレータの作成と動作確認

- `sdkmanager` で `emulator` と `system-images;android-35;google_apis;x86_64` をインストール
- `avdmanager` で `Pixel_5_API_35` AVD を作成
- `flutter emulators --launch Pixel_5_API_35` でエミュレータ起動
- `gradle.properties` に `android.overridePathCheck=true` を追加（日本語パス問題の回避）
- APKビルド成功 → ASCIIパスにコピー → `adb install` でインストール → `adb shell am start` で起動

### 動作確認結果（エミュレータ: Pixel 5 API 35）

- ✅ メイン画面: 「音声スケジュール」タイトル、`table_calendar` 月表示（日本語曜日・今日ハイライト・日曜赤）
- ✅ 手動追加（＋ボタン）→ 確認フォーム: タイトル・場所・日付（2026年7月1日(水)）・終日チェック・時刻 が表示
- ✅ 保存後: 一覧に「TestMeeting / 7月1日(水) 12:00」が表示され、カレンダーの7月1日にイベントドットが表示
- ✅ Drift（SQLite）への永続化が正常に動作

## 残作業

- [x] `flutter run` のパス問題の恒久対策 → 下記「10.」で解決
- [x] Riverpodのコード生成（`riverpod_generator`）の適用 → 下記「12.」で解決
- [x] Gitリポジトリの初期化 → 下記「11.」で完了（初回コミットは未実施）
- [x] `test/data/schedule_repository_test.dart` の作成 → 下記「12.」で完了（insert/query/ソート/ソフトデリート/update/watchAllの6テスト）

### 10. プロジェクトパスの恒久移動（`flutter run`パス問題の解決）

- まずジャンクション（`C:\dev\schedule_app` → 旧パス）を試したが、`flutter run`は内部でジャンクションを実パスに解決してしまい `aapt` が日本語パスでクラッシュする不具合が再現。ジャンクションでは根本解決にならないと判明
- ユーザーと相談の上、プロジェクト実体を **`C:\Users\rhoso\OneDrive\schedule-app`**（OneDriveルート直下、日本語を含む「ドキュメント」フォルダを経由しない場所）へ移動することに決定
  - `Move-Item`はハーネスの永続シェルが旧フォルダをカレントディレクトリとして保持しており失敗 → `robocopy /E`でコンテンツをコピーする方式に変更（`build` / `.dart_tool` / `android\.gradle` 等の再生成可能フォルダは除外指定、ただし`.gradle`は一部コピーされた＝実害なし）
  - 新パスで `flutter pub get` → `dart analyze` → `flutter test`（11/11パス）まで確認
  - 新パス直下の `build` フォルダも `C:\flutter_build_cache\schedule_app\build_out2` へジャンクションし、OneDrive同期対象から除外
  - **`flutter run -d emulator-5554 --debug`が新パスから完全に成功**（ビルド→インストール→起動→ホットリロード待受まで到達）。これでパス問題は恒久的に解決
  - 旧フォルダ（`OneDrive\ドキュメント\音声入力schedule-app`）は中身をほぼ削除済みだが、ハーネスの永続シェルがカレントディレクトリとして掴んでいるためフォルダ自体とジャンクション残骸1個は今回のセッション内で削除不可。実害はないが、後でエクスプローラーから手動削除可能
  - 以後、プロジェクトの作業ディレクトリは **`C:\Users\rhoso\OneDrive\schedule-app`**

### 11. Gitリポジトリの初期化

- ユーザー依頼により新パス（`C:\Users\rhoso\OneDrive\schedule-app`）で `git init` を実施
- `.gitignore`（ルート＋`android/.gitignore`）は`flutter create`標準のものがそのまま使え、`build/` `.dart_tool/` `android/.gradle` 等は除外済みであることを確認
- 初回コミットはまだ作成していない（ユーザーからの明示依頼待ち）

### 12. repositoryテストの追加とRiverpodコード生成の導入

- `test/data/schedule_repository_test.dart` を新規作成。`AppDatabase.forTesting(NativeDatabase.memory())` でインメモリDBを使い、以下6ケースを検証:
  - insert→getAllで取得できる／UUIDがクライアント生成されそれぞれ異なる／startAt昇順ソート／softDeleteでgetAllから消えるが行自体は残る（`deletedAt`が入る）／updateでフィールドとupdatedAtが更新される／watchAllがinsert後に新しいリストをemitする
  - 注意点: Driftの`updatedAt`列はSQLite上で秒単位精度になるため、update前後の時刻差テストは1秒超のディレイが必要だった（数十msのディレイでは差が出ずflaky）
- 併せて、スキャフォールド時のデフォルト `test/widget_test.dart`（存在しない`MyApp`カウンターアプリを参照して壊れていた雛形）を削除
- `lib/features/providers/schedule_providers.dart` を手動`Provider`/`StreamProvider`から `@riverpod` アノテーション方式に書き換え、`build_runner`で`schedule_providers.g.dart`を生成
  - `database`・`scheduleRepository`は元々アプリ全体で1つのDB接続を保持する設計だったため、デフォルトのautoDispose挙動を避けて `@Riverpod(keepAlive: true)` を明示
  - `scheduleEvents`（一覧のStreamProvider）はautoDisposeのままで問題なし
  - 生成されたプロバイダー名（`databaseProvider`/`scheduleRepositoryProvider`/`scheduleEventsProvider`）は既存の呼び出し側と一致するため、UI側のコード変更は不要だった
  - `Ref`型は`riverpod_annotation`単体ではエクスポートされておらず`flutter_riverpod`のimportが必要だった（`dart analyze`で発覚し追加）
- 新パスでの`build_runner`実行時も、`.dart_tool\build_resolvers`をOneDrive外（`C:\flutter_build_cache\schedule_app\build_resolvers2`）へジャンクションする対策を先に実施してから実行（旧パスで発生していたのと同じロック問題の再発を予防）
- 検証: `dart analyze lib/ test/` エラーなし、`flutter test`（17/17パス）、エミュレータでの`flutter run`実機起動も成功（プロバイダー配線変更後もクラッシュなし）

## 関連ファイル

- プラン: `C:\Users\rhoso\.claude\plans\rosy-orbiting-meteor.md`
- `C:\Users\rhoso\OneDrive\schedule-app\CLAUDE.md`
- `C:\Users\rhoso\OneDrive\schedule-app\lib\`
- `C:\Users\rhoso\OneDrive\schedule-app\.git\`（今回`git init`、未コミット）
- `C:\Users\rhoso\OneDrive\schedule-app\test\data\schedule_repository_test.dart`（新規）
- `C:\Users\rhoso\OneDrive\schedule-app\lib\features\providers\schedule_providers.dart` / `schedule_providers.g.dart`（Riverpodコード生成化）
- Junction: `.dart_tool\build_resolvers` → `C:\flutter_build_cache\schedule_app\build_resolvers2`（新パス用）
- Junction: `build` → `C:\flutter_build_cache\schedule_app\build_out2`（新パス用）
