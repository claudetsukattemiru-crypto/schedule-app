# 作業ログ 2026-06-28

## やったこと

1. **アプリ構想の整理**
   - 音声でスケジュールを登録できるアプリのアイデアを検討
   - コストを抑えるためモバイルアプリ（Flutter）で作る方針に決定
   - クラウド同期・複数デバイス間共有（自分の端末間のみ、複数ユーザー共有は対象外）は将来追加する前提で、データモデルだけ今から考慮

2. **実装プランの作成・承認**
   - プランファイル: `C:\Users\rhoso\.claude\plans\rosy-orbiting-meteor.md`
   - 構成: Flutter + `speech_to_text`（OS標準音声認識、無料） + Drift（ローカルsqlite） + Riverpod + `table_calendar`
   - v1スコープ: 日時解析はLLM API不使用のローカルルールベースのみ、クラウド同期は無し（バックエンド未選定）
   - `ScheduleEvent` データモデルは将来の同期を見据えてUUID主キー・UTCタイムスタンプ・ソフトデリート列を含む設計
   - 音声入力は必ず確認画面を経由してから保存（自動保存はしない）という方針

3. **CLAUDE.md作成**
   - `C:\Users\rhoso\OneDrive\ドキュメント\仮schedule-app\CLAUDE.md` に上記アーキテクチャ・コマンド・設計ルールを記録

4. **開発環境のセットアップ（途中まで）**
   - 開発機（Windows）の既存ツールを確認 → Flutter/Java/Android SDK/Android Studio すべて未導入だった（Gitのみ導入済み）
   - **Android Studio**: wingetでインストール完了（`C:\Program Files\Android\Android Studio`）
   - **Flutter SDK**: 公式GitHubリポジトリからstableブランチをクローンして導入完了（`C:\flutter`）
   - **Android SDK Command-line Tools**: Google公式リポジトリマニフェストから最新版（21.0）のWindows用URLを取得し、zipをダウンロード完了（`%TEMP%\cmdline-tools.zip`、約145MB）。まだ展開・配置はしていない

## 残作業（次回続き）

- [ ] `cmdline-tools.zip` を `%LOCALAPPDATA%\Android\Sdk\cmdline-tools\latest` に展開
- [ ] `sdkmanager` で platform-tools・build-tools・platforms（Android SDK本体）をインストールし、ライセンスに同意
- [ ] `Flutter` (`C:\flutter\bin`) と Android SDK関連パスを環境変数PATHに追加
- [ ] `flutter doctor` で最終確認（Android周り・ライセンス・JDK等のエラーが出れば対処）
- [ ] プラン記載の実装順序のステップ1（`ScheduleEvent`モデル + Driftデータベース + リポジトリ）から実装開始

## 関連ファイル

- プラン: `C:\Users\rhoso\.claude\plans\rosy-orbiting-meteor.md`
- `C:\Users\rhoso\OneDrive\ドキュメント\仮schedule-app\CLAUDE.md`
